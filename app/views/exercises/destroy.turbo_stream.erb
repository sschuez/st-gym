<%= turbo_stream.replace "user_#{current_user&.id}_exercises_index_header" do %>
  <%= render "shared/index_header", user: current_user, name: "Exercises", count: Exercise.all.count %>
<% end %>

<%= turbo_stream.replace "user_#{current_user&.id}_categories" do %>
  <%= render "exercises/sort_btns" %>
<% end %>

<%= turbo_stream.remove @exercise %>

<%= render_turbo_stream_flash_messages %>