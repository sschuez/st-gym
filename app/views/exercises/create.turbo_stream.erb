<%= turbo_stream.update "remote_modal", "" %>

<%= turbo_stream.replace "exercises_index_header" do %>
  <%= render "shared/index_header", name: "Exercises", count: Exercise.all.count %>
<% end %>

<%= turbo_stream.replace "categories" do %>
  <%= render "exercises/sort_btns" %>
<% end %>

<%= turbo_stream.prepend "exercises", @exercise %>
<%= render_turbo_stream_flash_messages %>