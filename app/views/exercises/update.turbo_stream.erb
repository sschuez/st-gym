<%= turbo_stream.update "remote_modal", "" %>

<%= turbo_stream.replace @exercise %>

<%= turbo_stream.replace "user_#{current_user&.id}_categories" do %>
  <%= render "exercises/sort_btns" %>
<% end %>

<%= render_turbo_stream_flash_messages %>